# Baked into every production build (next build) regardless of the host's own env,
# so a prod deploy can never silently fall back to the mock-mode default.
# .env.local still overrides for local experiments; a real host env var wins over both.
NEXT_PUBLIC_DEFAULT_MODE=chain
