/node_modules
/.next
/.next-*
/out
/build
*.tsbuildinfo
next-env.d.ts.timestamp
.DS_Store
.env*.local
npm-debug.log*

# telegram bot local store
/bot/data
/bot/.env

# Crown DB (SQLite) — data lives on the server, never in git
/data/

# local dev server (run-local.sh) — runtime artefacts of one machine
/.local-server.log
/.local-server.pid

# local debug scratch
np.js
.cdp-g.js
